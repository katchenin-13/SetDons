{# {% extends 'base-layout.html.twig' %}

{% block title %}Liste des audiences{% endblock %}
{% block header %}Audience{% endblock %}
{% block breadcrumb %}{% endblock %} #}
{% block body %}
<div class="card shadow-sm">
    <div class="card-header card-header-sm">
        <h3 class="card-title"> Liste des audiences</h3>
        <div class="card-toolbar">
            <a href="{{ path('audience.pdf') }}" class="btn btn-success btn-sm"  >
                        <i class="bi bi-plus-square text-light"></i>
                        imprimer
            </a>
                 &nbsp;
            <a href="{{ path('app_gestion_audience_new') }}" class="btn btn-main btn-sm"  
                data-bs-toggle="modal" data-bs-target="#exampleModalSizeLg2">
                <i class="bi bi-plus-square text-light"></i>
                Nouveau
            </a>
            {# {{ dump(permition)}} #}
        </div>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-sm-12">
                <div id="grid_app_gestion_audience" class="grid-dt-wrapper">Chargement....</div>
            </div>
        </div>
    </div>
</div>
{% endblock %}


{% block java %}
    <script src="{{ asset('assets/js/datatables.js') }}"></script>
    <script src="{{ asset('assets/js/active.js') }}"></script>
    
  
    <script>
        $(function () {
             
            $('#grid_app_gestion_audience').initDataTables({{ datatable_settings(datatable) }}, {
            searching: true,
            ajaxUrl: "{{ path('app_gestion_audience_index') }}",
            language: {
                url: asset_base_path + "/js/i18n/French.json"
            }
        });
     });
     
    </script>
{% endblock %}


